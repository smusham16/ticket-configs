server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_user
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: mysecretkey
  expiration: 3600000
